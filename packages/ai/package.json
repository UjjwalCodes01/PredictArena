{
  "name": "@predictarena/ai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The AI forecaster. Gemini estimates a probability, compares it to the market, and places a call only when it sees an edge — then is scored by exactly the same rules as every human player.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@google/genai": "^2.19.0",
    "@predictarena/db": "workspace:*",
    "@predictarena/dex": "workspace:*",
    "google-auth-library": "^10.2.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.11"
  }
}
